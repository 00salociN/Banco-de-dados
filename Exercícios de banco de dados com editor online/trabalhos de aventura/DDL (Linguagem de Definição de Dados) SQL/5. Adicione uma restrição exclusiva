Escreva uma consulta SQL para adicionar uma restrição exclusiva a uma coluna em uma tabela existente.

-- tabela existente
create table cliente(
    id int auto_increment,
    nomecliente varchar(100),
    idade int not null check (idade > -1),
    altura decimal(5,2) not null,
    CPF int,
    constraint PKcliente primary key(id)
);

-- colocando o campo com restrição para ser único
alter table cliente add constraint uc_cpf unique(CPF);